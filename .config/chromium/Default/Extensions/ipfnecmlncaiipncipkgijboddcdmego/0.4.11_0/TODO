popup.html:
  add "playlists" row

popup/init.js: prep_nav method:
  add click event for playlists
  create method for fetching list of playlists (duplicate fetch_album method)

myscript.js: onRequest method:
  add fetch_playlist action
    grab all li with data-type=pl 
      - or - 
    all li inside ul#playlists