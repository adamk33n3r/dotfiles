<br/>
<div id='data-addForm-content' class='row-fluid'>
    <div class='row-fluid'>
        <label class='span3 labels-vertical-pad'>Year *</label>

        <select class="span9"
            ng-model="selected_options.year"
            ng-options="year as year for year in years"
            ui-select2
        >
        </select>
    </div>
    <div class='row-fluid darken-select2-default'>
        <label class='span3 labels-vertical-pad'>Field *</label>

        <ui-select
            ng-model="selected_options.field"
            theme="select2"
            class="span9 clickable"
        >
            <ui-select-match placeholder="Select..."><% $select.selected.farm_name %> - <% $select.selected.field_name %></ui-select-match>
            <ui-select-choices repeat="field in fields | fieldFilter:$select.search">
                <div ng-bind-html="field.farm_name + ' - ' + field.field_name | highlight: $select.search"></div>
            </ui-select-choices>
        </ui-select>
    </div>
    <div class='row-fluid darken-select2-default'>
        <label class='span3 labels-vertical-pad'>Form *</label>
        <select class="span9"
            ng-model="selected_operation"
            ng-options="operation for operation in operations"
            ui-select2 data-placeholder='Select...'
        >
            <option value=''></option>
        </select>
    </div>
    <br/>
    <div>
        <button class="btn add-btn btn-primary grower-data-add-btn"
            ng-disabled="!selected_operation || !selected_options.field || !selected_options.year"
            ng-click="launchLegacyDialog(selected_operation, selected_field.field_id)"><i class="icon-plus"></i> Add Form</button>
    </div>
</div>
